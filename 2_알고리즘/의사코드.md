# 2. 알고리즘 (의사 코드) (10점)

## 1. `main` 함수의 전체 흐름
(프로젝트의 전체 작동 로직을 의사 코드로 작성하세요.)

(예시 - 관리 프로그램의 경우)
```
시작
    공정 정보 입력(input_processes)
    
    입력 순서 시뮬레이션 실행(simulate_sequence)
    입력 순서 평균 완료시간 계산

    공정 배열 복사
    SPT(처리시간 오름차순) 정렬(sort_by_time)

    SPT 시뮬레이션 실행(simulate_sequence)
    SPT 평균 완료시간 계산

    두 평균 시간 비교하여 결과 출력
끝
```

## 2. 핵심 기능 1: (예: 학생 등록 함수)
```
(핵심 기능 1의 논리적 절차를 작성하세요.)

시작
    공정 개수 입력받기
    만약 입력값이 1보다 작으면:
        공정 개수를 1로 설정
    만약 입력값이 최대 공정 수(MAX_PROCS)를 초과하면:
        공정 개수를 MAX_PROCS로 설정

    i = 0부터 공정 개수-1까지 반복:
        공정 이름 입력받기
        공정 소요시간 입력받기
        공정 시작시간(start)을 -1로 초기화
        공정 완료시간(finish)을 -1로 초기화
종료 

## 3. 핵심 기능 2: (예: 학생 검색 함수)
```
(핵심 기능 2의 논리적 절차를 작성하세요.)
```
시작
    현재 시간(time)을 0으로 초기화

    i = 0부터 공정 개수-1까지 반복:
        공정의 시작시간(start)을 현재 시간으로 설정
        현재 시간 = 현재 시간 + 공정 소요시간
        공정의 완료시간(finish)을 현재 시간으로 설정
        공정 이름, 시작시간, 소요시간, 완료시간을 출력

    총 소요된 현재 시간을 출력
종료