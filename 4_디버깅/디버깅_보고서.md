# 4. 디버깅 보고서 (10점)

> 💡 **디버깅이란?** 프로그램에서 발생한 오류(버그)를 찾아내고 고치는 과정입니다.

---

## 📝 작성 정보
- **작성 날짜:** 2025년 _11_월 _20_일
- **작성자:** 김주아

---

## 🐛 발견한 오류 #1

### 1) 어떤 문제가 발생했나요?
프로그램을 실행하면 입력값을 제대로 받지 못하고, 입력 오류가 발생해요.

```

### 2) 오류가 발생한 코드
#include <stdio.h>
#include <string.h>

#define MAX_PROCS 20
#define NAME_LEN 32

typedef struct {
    char name[NAME_LEN];
    int time;
    int start;
    int finish;
} Process;

void input_processes(Process procs[], int *n);

int main() {
    Process procs[MAX_PROCS];
    int n = 0;
    
    printf("공정 개수 입력 (최대 %d): ", MAX_PROCS);
    scanf("%d", n);  // 오류 발생: 주소를 넘기지 않음
    // 나머지 코드 생략
    return 0;
}

void input_processes(Process procs[], int *n) {
    // ...
}

```

### 3) 왜 오류가 생겼나요?
`scanf`에서 입력을 받을 때 변수의 주소를 넘겨야 하는데, 주소가 아닌 값 자체를 넘겨서 입력값을 제대로 받지 못했어요.


### 4) 어떻게 고쳤나요?
```c
#include <stdio.h>
#include <string.h>

#define MAX_PROCS 20
#define NAME_LEN 32

typedef struct {
    char name[NAME_LEN];
    int time;
    int start;
    int finish;
} Process;

void input_processes(Process procs[], int *n);

int main() {
    Process procs[MAX_PROCS];
    int n = 0;
    
    printf("공정 개수 입력 (최대 %d): ", MAX_PROCS);
    scanf("%d", &n);  // 주소를 넘기도록 수정
    // 나머지 코드 생략
    return 0;
}

void input_processes(Process procs[], int *n) {
    // ...
}


### 5) 고친 후 결과
- [] ✅ 잘 작동해요!
- [ ] ❌ 아직 문제가 있어요

---

## 🐛 발견한 오류 #2

### 1) 어떤 문제가 발생했나요?
```
공정 이름이 공백을 포함하면 제대로 입력되지 않아요.


### 2) 오류가 발생한 코드
```c
#include <stdio.h>
#include <string.h>

#define MAX_PROCS 20
#define NAME_LEN 32

typedef struct {
    char name[NAME_LEN];
    int time;
    int start;
    int finish;
} Process;

void input_processes(Process procs[], int *n);

int main() {
    Process procs[MAX_PROCS];
    int n = 0;
    
    printf("공정 개수 입력 (최대 %d): ", MAX_PROCS);
    scanf("%d", &n);
    
    for (int i = 0; i < n; i++) {
        printf("공정 %d 이름: ", i + 1);
        scanf("%s", procs[i].name);  // 오류 발생: 공백 포함된 이름 입력시 문제가 발생
    }
    return 0;
}

void input_processes(Process procs[], int *n) {
    // ...
}

```

### 3) 왜 오류가 생겼나요?
```
`scanf`는 공백을 포함한 문자열을 제대로 입력받지 못해 이름에 공백이 있을 경우 잘못 처리돼요.

```

### 4) 어떻게 고쳤나요?
```c
#include <stdio.h>
#include <string.h>

#define MAX_PROCS 20
#define NAME_LEN 32

typedef struct {
    char name[NAME_LEN];
    int time;
    int start;
    int finish;
} Process;

void input_processes(Process procs[], int *n);

int main() {
    Process procs[MAX_PROCS];
    int n = 0;
    
    printf("공정 개수 입력 (최대 %d): ", MAX_PROCS);
    scanf("%d", &n);
    
    for (int i = 0; i < n; i++) {
        printf("공정 %d 이름: ", i + 1);
        getchar();  // 남아있는 개행문자를 처리
        fgets(procs[i].name, NAME_LEN, stdin);  // fgets로 입력 받음
        procs[i].name[strcspn(procs[i].name, "\n")] = 0;  // 개행문자 제거
    }
    return 0;
}

void input_processes(Process procs[], int *n) {
    // ...
}

```

### 5) 고친 후 결과
- [ ] ✅ 잘 작동해요!
- [ ] ❌ 아직 문제가 있어요

---

## 🐛 발견한 오류 #3 (더 있으면 추가로 작성하세요)

### 1) 어떤 문제가 발생했나요?
프로그램이 공정 순서를 출력할 때, 예상과 다른 순서로 출력됩니다.

```

### 2) 오류가 발생한 코드
void simulate_sequence(Process procs[], int n) {
    int time = 0;
    for (int i = 0; i < n; ++i) {
        procs[i].start = time;
        time += procs[i].time;
        procs[i].finish = time;
        printf("공정 %d: %-10s | 시작: %3d 분, 소요: %3d 분, 완료: %3d 분\n",
               i+1, procs[i].name, procs[i].start, procs[i].time, procs[i].finish);
    }
    printf("총 소요 시간: %d 분\n", time);
}

```

### 3) 왜 오류가 생겼나요?
```
공정들이 입력된 순서대로 출력되지만, 가장 효율적인 처리 순서(SPT, Shortest Processing Time)가 적용되지 않아서 기대한 결과와 다르게 출력됩니다.

```

### 4) 어떻게 고쳤나요?
```c
void simulate_sequence(Process procs[], int n) {
    int time = 0;
    for (int i = 0; i < n; ++i) {
        procs[i].start = time;
        time += procs[i].time;
        procs[i].finish = time;
        printf("공정 %d: %-10s | 시작: %3d 분, 소요: %3d 분, 완료: %3d 분\n",
               i+1, procs[i].name, procs[i].start, procs[i].time, procs[i].finish);
    }
    printf("총 소요 시간: %d 분\n", time);
}

// 정렬된 공정으로 시뮬레이션을 실행해야 하므로, 이를 위한 수정이 필요합니다.


### 5) 고친 후 결과
- [ ] ✅ 잘 작동해요!
- [ ] ❌ 아직 문제가 있어요

---

## 🧪 프로그램 테스트 결과

### 테스트 1
- **무엇을 테스트했나요?** 공정 개수와 이름 입
- **어떻게 테스트했나요?** 공정 개수를 3으로 입력하고, 이름을 공백 포함해서 입력했어요.
- **결과:** ✅ 성공 / ❌ 실패

### 테스트 2
- **무엇을 테스트했나요?** 처리 시간에 따른 공정 순서 시뮬레이션
- **어떻게 테스트했나요?** 처리 시간이 짧은 순서대로 공정을 정렬하고, 시뮬레이션 결과를 출력했어요.
- **결과:** ✅ 성공 / ❌ 실패

---

## 💭 디버깅하면서 배운 점

- `scanf`와 `fgets`의 차이를 알게 되었고 공백을 포함한 입력을 할 때는 fgets를 사용하는 것이 더 안전하다
- 배열을 다룰 때 범위를 벗어나지 않도록 조심해야 한다는 것
- 코드에서 잘못된 순서로 출력되는 문제를 디버깅하면서 정렬을 미리 해주는 것이 중요함


---

## ✅ 최종 체크리스트

프로그램이 잘 작동하는지 최종 확인해보세요!

- [ ] 컴파일이 잘 되나요? (오류 메시지가 없나요?)
- [ ] 프로그램이 실행되나요?
- [ ] 메뉴가 제대로 나오나요?
- [ ] 입력한 값이 정상적으로 처리되나요?
- [ ] 모든 기능이 잘 작동하나요?
